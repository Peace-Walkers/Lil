let a = 15
let b = 9

mut ages = {10, 5, 3}

io::println("before:")
io::println(ages)

ages.push(b)
ages.push(a)

io::println("after:")
io::println(ages)

let doubled = ages.map(|a| a * 2)

io::println("doubled:")
io::println(doubled)

let filtred = ages.map(|a| a *2).filter(|a| a < 15)

io::println("doubled filtred:")
io::println(filtred)

io::println("je test des trucs")

io::print(doubled.len() - filtred.len())
io::println(" person are older than 15")
