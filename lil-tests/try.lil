
fn! info_read(path):
  let content = io::read(path)?
  let infos = fs::stat(path)
  let infos = {
      file: content,
      infos: infos,
  }
  return infos


let build_zig = info_read("build.zig")?

io::println(build_zig)
